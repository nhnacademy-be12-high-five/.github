name: Infra 공용 배포

on:
  workflow_call:
    inputs:
      service-name:
        required: true
        type: string
      container-name:
        required: true
        type: string
      compose-file:
        required: false
        type: string
        default: "docker-compose.infra.yml"

    secrets:
      DOCKER_USERNAME:
        required: true
      DOCKER_PASSWORD:
        required: true
      SSH_IP:
        required: true
      SSH_ID:
        required: true
      SSH_PW:
        required: true
      SSH_PORT:
        required: true

jobs:
  deploy-infra:
    runs-on: ubuntu-latest

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: JDK 21 설정
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Maven 빌드
        run: mvn clean package -DskipTests

      - name: Docker 로그인
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker 이미지 빌드 및 푸시
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/${{ inputs.service-name }}:latest

      - name: 서버에 배포
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_IP }}
          username: ${{ secrets.SSH_ID }}
          password: ${{ secrets.SSH_PW }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            echo "🚀 ${{ inputs.service-name }} 배포 시작..."
            cd ~/www/nhnbook

            CONTAINER_NAME="${{ inputs.container-name }}"
            SERVICE_NAME="${{ inputs.service-name }}"
            COMPOSE_FILE="${{ inputs.compose-file }}"

            docker stop $CONTAINER_NAME || true
            docker rm $CONTAINER_NAME || true

            docker-compose -f $COMPOSE_FILE pull $SERVICE_NAME
            docker-compose -f $COMPOSE_FILE up -d $SERVICE_NAME

            # 헬스 체크 포트 결정
            PORT=8080
            if [ "$SERVICE_NAME" = "eureka_server" ]; then
              PORT=10481
            elif [ "$SERVICE_NAME" = "config_server" ]; then
              PORT=8888
            fi

            HEALTHY=false
            for i in {1..10}; do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:$PORT/actuator/health || echo "000")
              if [ "$STATUS" == "200" ]; then
                HEALTHY=true
                break
              fi
              echo "⏳ 시도 $i: 아직 준비되지 않음 ($STATUS)"
              sleep 5
            done

            if [ "$HEALTHY" != true ]; then
              echo "❌ 배포 실패: 컨테이너가 정상 상태가 아닙니다."
              exit 1
            fi

            docker image prune -f
            echo "✅ ${{ inputs.service-name }} 배포 완료!"
