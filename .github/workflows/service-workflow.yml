name: MSA 서비스 공용 워크플로우

on:
  workflow_call:
    inputs:
      project-key:
        required: true
        type: string
      docker-image:
        required: true
        type: string
      container-name:
        required: true
        type: string
      maven-args:
        required: false
        type: string
        default: ""
      # [추가됨] 배포 실행 여부 (기본값: true -> 기존 서비스 영향 없음)
      deploy-enabled:
        required: false
        type: boolean
        default: true
    secrets:
      SONAR_TOKEN:
        required: true
      DOCKER_USERNAME:
        required: true
      DOCKER_PASSWORD:
        required: true
      SSH_IP:
        required: true
      SSH_ID:
        required: true
      SSH_PW:
        required: true
      SSH_PORT:
        required: true

jobs:
  build_deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 코드 체크아웃 (항상 실행)
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      # 2. JDK 설정 (항상 실행)
      - name: JDK 21 설정
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # 3. Maven 빌드 + SonarQube 분석 (항상 실행)
      - name: Maven 빌드 및 SonarQube 분석
        run: |
          if [ "${{ github.ref_name }}" == "main" ]; then
            DYNAMIC_PROJECT_KEY="${{ inputs.project-key }}"
          else
            SAFE_BRANCH_NAME=$(echo "${{ github.ref_name }}" | sed 's/\//-/g')
            DYNAMIC_PROJECT_KEY="${{ inputs.project-key }}-$SAFE_BRANCH_NAME"
          fi

          echo "ℹ️ [정보] 현재 브랜치: ${{ github.ref_name }}"
          echo "ℹ️ [정보] SonarQube Project Key: $DYNAMIC_PROJECT_KEY 로 분석을 시작합니다."

          mvn clean package sonar:sonar \
            -Dsonar.projectKey=$DYNAMIC_PROJECT_KEY \
            -Dsonar.host.url=http://s4.java21.net:9000 \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }} \
            ${{ inputs.maven-args }}

      # 4. Docker Hub 로그인 (배포 옵션 켜졌을 때만 실행)
      - name: Docker Hub 로그인
        if: ${{ inputs.deploy-enabled }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 5. Docker 이미지 빌드 및 푸시 (배포 옵션 켜졌을 때만 실행)
      - name: Docker 이미지 빌드 및 푸시
        if: ${{ inputs.deploy-enabled }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ inputs.docker-image }}:latest

      # 6. 서버 접속 및 배포 (배포 옵션 켜졌을 때만 실행)
      - name: 서버 배포 (SSH, Rolling)
        if: ${{ inputs.deploy-enabled }}
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_IP }}
          username: ${{ secrets.SSH_ID }}
          password: ${{ secrets.SSH_PW }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            echo "🚀 ${{ inputs.container-name }} 서비스 배포 시작 (Rolling Update)..."
            cd ~/www/nhnbook

            SERVICE_BASE="${{ inputs.container-name }}"

            for INSTANCE in 1 2; do
              CONTAINER_NAME="${SERVICE_BASE}_${INSTANCE}"
              echo "🔄 $CONTAINER_NAME 컨테이너 업데이트 중..."

              # 최신 이미지를 pull 받고 컨테이너를 재생성합니다.
              docker compose pull $CONTAINER_NAME
              docker compose up -d $CONTAINER_NAME
            
              # (선택사항) 새 컨테이너가 뜨는 시간을 잠깐 기다려줍니다.
              sleep 5
            done

            # 게이트웨이 헬스체크를 통해 전체 서비스 정상 가동 확인
            echo "🏥 전체 서비스 헬스체크 시작 (Gateway)..."
            HEALTHY=false
            for i in {1..12}; do # 약 1분간 대기 (5초 * 12회)
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:10485/actuator/health || echo "000")
              if [ "$STATUS" == "200" ]; then
                HEALTHY=true
                echo "✅ 헬스체크 성공 (HTTP 200 OK)"
                break
              fi
              echo "⏳ [$i/12] 대기 중... (현재 상태: $STATUS)"
              sleep 5
            done

            if [ "$HEALTHY" != true ]; then
              echo "❌ 배포 실패: Gateway 헬스체크 타임아웃"
              exit 1
            fi

            docker image prune -f
            echo "✅ ${{ inputs.container-name }} 서비스 배포 완료!"